# @package _global_
# Baseline experiment: ResNet18 + temporal average pooling
defaults:
  - override /model: Dinov

model:
  pretrained: true
  backbone : "facebook/dinov3-vith16plus-pretrain-lvd1689m"